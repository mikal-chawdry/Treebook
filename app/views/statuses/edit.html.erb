<h1>Editing status</h1>

<%= render 'form' %> <!-- the edit and show views both share the same form. Having a partial allows us to create the form once and use it in multiple views. -->

<%= link_to 'Show', status_path(@status.id) %> |
<%= link_to 'Back', statuses_path %>
